#Binary-search-tree

##JavaScript

###Methods

- ```
  buidldTree(arr)
  ```
- ```
  insert(value)
  ```
  - ```
    delete(node)
    ```
  ````
  - ```
  find(value)
  ````
  - ```
    buidldTree(arr)
    ```
  ````
  - ```
  buidldTree(arr)
  ````
  - ```
    buidldTree(arr)
    ```
  ````
  - ```
  buidldTree(arr)
  ````
  - ```
    buidldTree(arr)
    ```
